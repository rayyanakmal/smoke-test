# Smoke Test: Hello API

Build a FastAPI endpoint that returns "hello world" with:
- GET /hello returns {"message": "hello world"}
- GET /health returns {"status": "ok"}
- Tests at 80%+ coverage
- README with curl examples
